<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Quản lý</title>
</head>

<body>
    <div class="container">
        <div class="d-flex justify-content-between mt-3">
            <a class="fw-bold text-decoration-none text-black me-3 size-26 h3" href="quanly">Quản lý bán hàng</a>
            <a class="fw-bold text-decoration-none text-black me-3" href="login">Đăng xuất</a>
        </div>
        <div class="row mt-5 w-100 ">
            <form class="w-100" method="POST" id="form_update" enctype="multipart/form-data"
                action="/api/editUser/{{id}}">
                <div class="row d-flex justify-content-center">
                    <div class="col-4 input-group mb-3">
                        <span class="input-group-text w-20" id="basic-addon3">Email</span>
                        <input type="email" class="form-control" id="email" value="{{email}}" name="email"
                            placeholder="" required />
                    </div>
                </div>
                <div class="row d-flex mt-3 justify-content-center">
                    <div class="col-4 input-group mb-3">
                        <span class="input-group-text w-20" id="basic-addon3">Họ và tên</span>
                        <input type="text" class="form-control" id="fullname" value="{{name}}" name="hoTen"
                            placeholder="" required />
                    </div>
                </div>
                <div class="row mt-3 d-flex justify-content-center">
                    <div class="col-4 input-group mb-3 ">
                        <span class="input-group-text w-20" id="basic-addon3">Mật khẩu</span>
                        <input type="password" class="form-control" name="pass" placeholder="" required />
                    </div>
                </div>
                <div class="row mt-3">
                    <div class="col-4 input-group mb-3">
                        <input type="file" name="myFile" accept="image/*" class="form-control w-20" required />
                    </div>
                </div>

                <div class="row mt-3">

                    <div class="col-4 "><button type="submit" class="btn btn-warning text-light fw-bold">Update
                            người dùng </button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</body>

</html>